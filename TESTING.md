# Smoke Test Checklist — คืนหอนหลอนหมาป่า

> ใช้ก่อนปล่อยทุกครั้งหลังแก้โค้ด · P0 ของโปรเจค
> ทำเครื่องหมาย `[x]` เมื่อผ่าน · จดบันทึกบั๊กท้ายเอกสารนี้

---

## 0. วิธีเตรียม environment

### 0.1 ทดสอบอัตโนมัติ (รันก่อนเสมอ)

```bash
npm test          # หรือ: node test/smoke.mjs
```

- ไม่ต้องติดตั้ง dependency — ใช้ Node http server + Chrome DevTools Protocol ตรง ๆ (ต้องมี Chrome/Edge)
- ครอบคลุม **42 checks**: boot/theme/setup/manual assign/reveal/เล่นจบเกมครบทุกเฟส/boss ทุกบทบาท,
  บันทึก-เล่นต่อ (reload กลางเกม), undo แขวนคอ, moderator panel, history, XSS, จับเวลา,
  dialog ของแอปแทน native `confirm()`/`alert()`, PWA (SW + cache + ออฟไลน์),
  ไม่มี JS exception / 404
- ผ่านทั้งหมด = พร้อม deploy · ถ้า fail ดูบรรทัด `ไม่ผ่าน:` ท้ายผลลัพธ์

### 0.2 ทดสอบ Manual (ตาม checklist ด้านล่าง)

| วิธี | คำสั่ง | ใช้ทดสอบอะไร |
|---|---|---|
| เปิดตรง ๆ | ดับเบิลคลิก `index.html` | ตัวเกม (ไม่มี service worker — ทดสอบไม่ได้) |
| Local server | `npm run serve` แล้วเปิด `http://127.0.0.1:8123/` | ตัวเกม + PWA/Offline |
| เครื่องจริง | host ขึ้น HTTPS (GitHub Pages / Netlify) แล้วเปิดบนมือถือ | PWA, Wake Lock, Haptic, Add to Home Screen |

> ⚠️ Service worker ทำงานเฉพาะ `https:` หรือ `localhost` เท่านั้น

---

## 1. ตั้งค่าและแจกบทบาท

- [ ] เปิดแอป → หน้าแรกแสดงโลโก้ + ปุ่ม "เริ่มเกมใหม่" / "ตั้งค่าเกม" / "วิธีใช้"
- [ ] จำนวนผู้เล่นเปลี่ยนได้ 4–18 คน (ปุ่ม − ปิดที่ 4, + ปิดที่ 18)
- [ ] ตั้งชื่อผู้เล่นได้ + ชื่อว่างถูกแทนด้วย "ผู้เล่น N"
- [ ] เพิ่ม/ลดบทบาทได้ · บทบาท singleton ขึ้น "(สูงสุด 1)" และกด + ไม่ได้เมื่อเต็ม
- [ ] จำนวนบทบาทเกินจำนวนผู้เล่น → ปุ่ม + ถูกปิด
- [ ] ไม่มีหมาป่า → ขึ้น ⚠ เตือน + ปุ่มเริ่มเกมถูก disable
- [ ] หมาป่า ≥ ชาวบ้าน → ⚠ เตือน + ปุ่มเริ่มเกมถูก disable
- [ ] โหมด 🎲 สุ่มอัตโนมัติ → เข้าหน้าแจกการ์ดทันที
- [ ] โหมด ✋ กำหนดเอง → เข้าหน้า "จัดบทบาท" · เปลี่ยนบทบาทได้ · validator บอก "บทบาทครบพอดี"
- [ ] ปุ่ม "สุ่มใหม่" / "ล้างบทบาท" (ขึ้น dialog ยืนยันของแอป) ทำงาน
- [ ] Preset: กด `⚖️ มาตรฐาน` / `🎉 Party` / `🏆 Competitive` → บทบาทเปลี่ยนตาม · ชิป 4/6/8/10/12/14/16 คน → เปลี่ยนจำนวน+ชุดตาม · บันทึกชุดปัจจุบัน → แสดงในรายการ → ใช้ ✓ / ลบ ✕ (มี dialog ยืนยัน)
- [ ] หน้าแจกการ์ด (Reveal) → กดถัดไปทีละคน มีจุดแสดงความคืบหน้า + นับ "ตรวจแล้ว N" · หมาป่าเห็นชื่อหมาป่าด้วยกัน

## 2. กลางคืน — เรียกครบทุกบทบาท

- [ ] หน้า Night Panel แสดงเฉพาะบทบาทที่ยังมีชีวิต + เปอร์เซ็นต์ความคืบหน้า + "ลำดับที่แนะนำ"
- [ ] 🐺 หมาป่าเลือกเหยื่อ (มีโหมด 2 เป้าหมายเมื่อเข้าเงื่อนไข) → สรุปสรุปถูกต้อง
- [ ] 👁️ ผู้หยั่งรู้ตรวจ 1 คน → ผลถูกต้อง (หมาป่า/ไม่ใช่) + บันทึกใน History
- [ ] 💊 แม่มด: ใช้ยารักษาได้ 1 ครั้ง · ใช้ยาพิษได้ 1 ครั้ง · ใช้ได้แค่ครั้งละ 1 อย่าง · ใช้หมดแล้วปุ่มถูกปิด
- [ ] 💉 หมอ / 🛡️ บอดี้การ์ด กัน 1 คนได้
- [ ] 💘 คิวปิด (คืนแรก) เลือกคู่รัก 2 คน
- [ ] 👵 ยายแก่ขับไล่ 1 คน → คนนั้นถูกตัดสิทธิ์โหวตวันนั้น
- [ ] 🌀 ผู้ต้องสาปถูกกัด → กลายเป็นหมาป่า + แจ้งลับตอนรุ่งเช้า
- [ ] ⏪ **Undo** ได้ทุกบทบาท (หมาป่า/หยั่งรู้/หมอ/บอดี้การ์ด/คิวปิด/ยาพิษ/ยายแก่)
- [ ] สรุปกลางคืน (dialog ของแอป) แสดงเหตุผลการตายครบทุกกรณี: ถูกกัด / ยาพิษ / ยิง / ตายตามคู่รัก / รอดเพราะหมอ-บอดี้การ์ด-แม่มด / รอดเพราะผู้ป่วยติดเชื้อ

## 3. รุ่งเช้าและนายพราน

- [ ] หน้า Dawn แสดงรายการเสียชีวิตพร้อม "สาเหตุ" ที่ถูกต้อง หรือ "ไม่มีผู้เสียชีวิต"
- [ ] นายพรานตายตอนกลางคืน → ปุ่ม "🎯 นายพรานจะยิง" → เลือกยิง 1 คน หรือ "ไม่ยิง"
- [ ] การยิงของนายพรานฆ่าเป้าหมาย + ตรวจแพ้-ชนะทันที
- [ ] ผู้ต้องสาปกลายร่าง/ยายแก่ขับไล่/ผู้ป่วยติดเชื้อ แสดง notice ถูกต้อง

## 4. กลางวันและการโหวต

- [ ] หน้ากลางวันแสดงรายชื่อคนเป็น + จับเวลาอภิปราย
- [ ] โหวตทีละคน: เลือกคนโหวต → เลือกเป้าหมาย → ยืนยัน ·  Undo รายคนได้ · ล้างทั้งหมด (ขึ้น dialog ยืนยันของแอป)
- [ ] ปุ่ม "ข้าม" สำหรับคนไม่โหวต
- [ ] 🎖️ นายอำเภอโหวตมีน้ำหนัก x2 (ตรวจผลนับคะแนน)
- [ ] 👑 เจ้าชายโดนโหวตครั้งแรก → ไม่ตาย + เปิดบทบาททันที (หน้า Prince)
- [ ] เสมอ → เลือก "โหวตใหม่" หรือ "ไม่แขวน"
- [ ] แขวนคอ → เหตุผลถูกต้อง · **ปุ่ม "↩︎ ย้อนการแขวน" ใช้ได้ถ้าเกมยังไม่จบ**
- [ ] 🃏 คนโง่ถูกโหวตออก → **จบเกมทันที คนโง่ชนะ**
- [ ] ทุกครั้งที่มีคนตาย → ตรวจแพ้-ชนะทันที (ไม่ต้องรอจบเฟส)

## 5. เคสชนะ (Win conditions)

- [ ] หมาป่าตายหมด → 🏘️ ชาวบ้านชนะ
- [ ] หมาป่า ≥ ชาวบ้าน → 🐺 หมาป่าชนะ
- [ ] คู่รักต่างฝ่ายเหลือ 2 คนสุดท้าย → 💘 คู่รักชนะ
- [ ] คนโง่ถูกโหวตออก → 🃏 คนโง่ชนะ
- [ ] หน้าจบเกม: ตารางบทบาทครบ + สถานะถูกต้อง (ลูกหมาป่า/ผู้ต้องสาป/เจ้าชาย มี marker)
- [ ] "📋 คัดลอกผลลัพธ์" คัดลอกข้อความลง clipboard ได้ (และ fallback ทำงานเมื่อไม่รองรับ)
- [ ] "เล่นใหม่" / "กลับหน้าหลัก" ทำงาน

## 6. บันทึกเกม (Save / Resume)

- [ ] เล่นกลางคัน → กด ⌂ ออก → ขึ้น dialog "เกมจะถูกบันทึกไว้"
- [ ] กลับหน้าแรก → แสดง "💾 มีเกมค้างอยู่ · เฟส X วันที่ N · M คน"
- [ ] "เล่นเกมต่อ" กลับเข้าเฟสเดิม **พร้อมข้อมูลทั้งหมด** (ผลตรวจ Seer, คะแนนโหวต, ยาที่ใช้แล้ว, log)
- [ ] "ลบข้อมูลเก่า" (ขึ้น dialog ยืนยันของแอป) ล้างจริง
- [ ] ทดสอบ Resume ตั้งแต่ทุกเฟส: assign / reveal / night / dawn / voting / execution
- [ ] localStorage ถูกบล็อก (Safari private) → แอปไม่ crash

## 7. PWA / Offline

- [ ] เปิดผ่าน `localhost` หรือ HTTPS → Console ไม่มี error ของ service worker
- [ ] DevTools → Application → Service Workers: **activated and running**
- [ ] Cache Storage มี `werewolf-shell-v1.0.0` พร้อมไฟล์ครบ
- [ ] เปิด DevTools → Network → **Offline** → รีเฟรชหน้า → แอปยังโหลดและทำงานได้
- [ ] ปิดอินเทอร์เน็ตจริงบนมือถือ → เปิดแอปจากหน้าจอหลักได้
- [ ] Chrome/Edge: ไอคอนติดตั้ง PWA ถูกต้อง (manifest ไม่ error)
- [ ] iOS: เพิ่มลงหน้าจอหลัก → ไอคอน 180×180 แสดงถูก + เปิดแบบ standalone
- [ ] แก้ไข `index.html` แล้วเปิดซ้ำ 2 ครั้ง → ได้เวอร์ชันใหม่ (SW อัปเดตอัตโนมัติ)

## 8. UX พื้นฐาน

- [ ] ธีม 🌙 Dark / ☀️ Light / 🔄 Auto เปลี่ยนจริง + จำค่าไว้
- [ ] ขนาดตัวอักษร ปกติ/ใหญ่/ใหญ่มาก เปลี่ยนจริง + จำค่าไว้
- [ ] ⏱ จับเวลา: เริ่ม/หยุด/รีเซ็ต/presets +  beep เมื่อหมด + pill แสดงบน topbar
- [ ] 📳 สั่นแจ้งเตือนทำงานบนมือถือ (เปิด-ปิดได้)
- [ ] 🔓 Wake Lock: หน้าจอดับไม่ได้ระหว่างเกม (เมื่อเปิดตัวเลือก)
- [ ] Moderator Panel (กดค้าง 3 วินาที) → เห็นบทบาท/คู่รัก/ยายแก่ ครบทุกเฟส
- [ ] 📜 History เรียงตามวัน + มีผลตรวจ Seer
- [ ] ชื่อผู้เล่นที่มีอักขระพิเศษ (`<script>`, `"` `'`) ไม่ break หน้าจอ (ทดสอบ XSS)
- [ ] หน้าจอขนาดเล็ก (iPhone SE / มือถือ 5") ไม่มีปุ่มล้นขอบ · safe-area ไม่บังปุ่มล่าง

---

## บันทึกผลการทดสอบ

| วันที่ | ผู้ทดสอบ | เวอร์ชัน | ผล | บั๊กที่พบ |
|---|---|---|---|---|
| _เพิ่มแถวใหม่ที่นี่_ | | | | |

### บั๊กที่พบ (เปิด issue / แก้แล้วติ๊ก)

- [ ] …
